<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= it.title %> - <%= it.siteTitle %></title>
   <link
      rel="stylesheet"
      href="https://unpkg.com/@picocss/pico@latest/css/pico.min.css"
    />
</head>
<body>
  <nav class="container">
    <a href="<%= it.baseUrl %>"><strong><%= it.siteTitle %></strong></a>
  </nav>

  <main class="container">
    <%~ include(it.partial || "partials/header", it) %>
    <%~ it.content %>
  </main>
</body>
</html>
